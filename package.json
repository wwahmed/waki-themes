{
  "name": "waki-themes",
  "version": "1.2.1",
  "private": true,
  "description": "A swappable Waki theme catalog with a versioned JSON bundle for cross-app consumption via the dist/themes.json contract.",
  "license": "MIT",
  "scripts": {
    "start": "python3 -m http.server 5500",
    "open": "open http://localhost:5500",
    "build": "node scripts/build-bundle.mjs",
    "gen:v2": "node scripts/gen-v2-themes.mjs",
    "studio:dev": "cd app && npm install && npm run dev",
    "studio:build": "node scripts/build-bundle.mjs && cd app && npm install && npm run build",
    "studio:preview": "cd app && npm run preview"
  },
  "keywords": [
    "theme",
    "css",
    "glassmorphism",
    "frosted-glass",
    "tailwind",
    "design-system"
  ]
}
